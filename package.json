{
  "name": "@faultkey/causallayer-mcp",
  "version": "0.5.0",
  "description": "Air-gapped, pure-Node Model Context Protocol server for FaultKey · CausalLayer — deterministic AI liability attribution. Runs entirely offline via stdio; never opens a network socket. For end-users who want a one-line connection to the public demo, use the unscoped `causallayer-mcp` package instead.",
  "private": false,
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/standalone-mcp-stdio.js",
  "bin": {
    "causallayer-mcp": "./dist/standalone-mcp-stdio.js",
    "faultkey-mcp": "./dist/standalone-mcp-stdio.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE",
    "server.json"
  ],
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "ai-governance",
    "ai-liability",
    "ai-incident-response",
    "causal-attribution",
    "audit-trail",
    "merkle-tree",
    "ed25519",
    "opentimestamps",
    "cloudflare-workers",
    "deterministic",
    "bitcoin-anchor",
    "insurance",
    "compliance"
  ],
  "homepage": "https://faultkey.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/smq9sn5jck-coder/causallayer-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/smq9sn5jck-coder/causallayer-mcp/issues"
  },
  "scripts": {
    "build:npm": "esbuild src/standalone-mcp-stdio.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/standalone-mcp-stdio.js --banner:js=\"#!/usr/bin/env node\"",
    "prepublishOnly": "pnpm run build:npm",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:sandbox": "wrangler deploy --env sandbox",
    "deploy:production": "wrangler deploy --env production",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.12.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "@types/node": "^22.7.4",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.7",
    "wrangler": "^4.93.0"
  },
  "engines": {
    "node": ">=20"
  },
  "mcpName": "io.github.smq9sn5jck-coder/causallayer",
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.24.3",
      "ws": ">=8.20.1",
      "vite": ">=6.4.2"
    }
  }
}
