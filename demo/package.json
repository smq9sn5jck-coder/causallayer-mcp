{
  "name": "faultkey-try-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Cloudflare Worker for the FaultKey interactive demo (deterministic liability scoring engine).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
